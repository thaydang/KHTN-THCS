% Pandoc XeLaTeX template cho tài liệu KHTN tiếng Việt
\documentclass[12pt]{article}
\usepackage{fontspec}
\usepackage{polyglossia}
\setmainlanguage{vietnamese}
\setotherlanguage{english}
\setmainfont{Times New Roman}
\setsansfont{Arial}
\setmonofont{Fira Code}
\usepackage{geometry}
\geometry{margin=2.5cm}
\usepackage{setspace}
\setstretch{1.3}
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=blue, citecolor=teal, urlcolor=magenta}
\usepackage{csquotes}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{enumitem}
\setlist{nosep, wide=0pt, leftmargin=1.5em}

% Macro bổ sung cho CV7991
\newenvironment{learningobjectives}{\section*{Mục tiêu học tập}}{}
\newenvironment{competencies}{\section*{Phát triển năng lực}}{}
\newenvironment{teachingactivities}{\section*{Hoạt động dạy học}}{}
\newenvironment{assessment}{\section*{Đánh giá}}{}

$if(title)$
  \title{$title$}
$endif$
$if(subtitle)$
  \providecommand{\subtitle}[1]{\posttitle{\par\large#1}}
  \subtitle{$subtitle$}
$endif$
$if(author)$
  \author{$for(author)$$author.name$$if(author.affiliation)$\\$author.affiliation$$endif$$sep\\$endfor$}
$endif$
$if(date)$
  \date{$date$}
$endif$

\begin{document}
$if(title)$
  \maketitle
$endif$

$if(toc)$
  {\setcounter{tocdepth}{2}\tableofcontents}
  \newpage
$endif$

$body$

$if(bibliography)$
  \newpage
  \printbibliography[title={Tài liệu tham khảo}]
$endif$
\end{document}
